apiVersion: v1
kind: Service
metadata:
  name: api-pagamentos
  namespace: producao
  labels:
    app: api-pagamentos
spec:
  type: ClusterIP
  selector:
    app: api-pagamentos
  ports:
    - name: http
      port: 8080
      targetPort: 8080
